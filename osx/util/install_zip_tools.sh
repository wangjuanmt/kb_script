#!/usr/bin/env bash

# BetterZip (QuickLook plugin is already included)
# (iZip.app installed by brew cask is broken)
#brew cask install betterzip #betterzipql #the-unarchiver #izip
#brew fetch --retry --force-bottle --deps p7zip unrar
brew install p7zip unrar
