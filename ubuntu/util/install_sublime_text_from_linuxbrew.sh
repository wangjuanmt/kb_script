#!/usr/bin/env bash

#brew install athrunsun/linuxbinary/sublime-text

# Although we can input CJK characters with IME fix, sometimes it crashes, thus NOT ideal.
# NOTE this depends on formula "sublime-text".
brew install athrunsun/linuxbinary/sublime-text-with-ime-fix
