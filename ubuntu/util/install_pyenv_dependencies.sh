#!/usr/bin/env bash

# Install recommended dependencies
# https://github.com/yyuu/pyenv/wiki

# Install dependencies using linuxbrew
#brew install bzip2 openssl readline sqlite xz

# Install dependencies using apt
sudo apt-get install -y make build-essential libssl-dev zlib1g-dev libbz2-dev libreadline-dev libsqlite3-dev wget curl llvm libncurses5-dev xz-utils libffi-dev
